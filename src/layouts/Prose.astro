---
import Default from './Minimal.astro';

interface Props {
	title: string;
}

const { title } = Astro.props;
---

<Default title={title}>
	<article class="prose lg:prose-xl m-auto mt-8">
		<slot />
	</article>
</Default>
